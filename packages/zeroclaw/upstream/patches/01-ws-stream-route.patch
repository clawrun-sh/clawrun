diff --git a/src/gateway/mod.rs b/src/gateway/mod.rs
index 4d27574..42ffe70 100644
--- a/src/gateway/mod.rs
+++ b/src/gateway/mod.rs
@@ -13,6 +13,7 @@ mod openclaw_compat;
 pub mod sse;
 pub mod static_files;
 pub mod ws;
+pub mod ws_stream;
 
 use crate::channels::{
     BlueBubblesChannel, Channel, GitHubChannel, LinqChannel, NextcloudTalkChannel, QQChannel,
@@ -868,6 +869,8 @@ pub async fn run_gateway(host: &str, port: u16, config: Config) -> Result<()> {
         .route("/api/events", get(sse::handle_sse_events))
         // ── WebSocket agent chat ──
         .route("/ws/chat", get(ws::handle_ws_chat))
+        // ── Streaming WebSocket agent chat ──
+        .route("/ws/stream", get(ws_stream::handle_ws_stream))
         // ── Static assets (web dashboard) ──
         .route("/_app/{*path}", get(static_files::handle_static))
         // ── Config PUT with larger body limit ──
