diff --git a/Cargo.toml b/Cargo.toml
index 92a063f..0603a0a 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -126,6 +126,13 @@ probe-rs = { version = "0.30", optional = true }
 pdf-extract = { version = "0.10", optional = true }

 # Raspberry Pi GPIO / Landlock (Linux only) — target-specific to avoid compile failure on macOS
+# --- CloudClaw: Postgres memory backend ---
+tokio-postgres = { version = "0.7", features = ["with-serde_json-1", "with-chrono-0_4"], optional = true }
+deadpool-postgres = { version = "0.14", optional = true }
+postgres-types = { version = "0.2", features = ["derive"], optional = true }
+pgvector = { version = "0.4", features = ["postgres"], optional = true }
+tokio-postgres-rustls = { version = "0.12", optional = true }
+
 [target.'cfg(target_os = "linux")'.dependencies]
 rppal = { version = "0.22", optional = true }
 landlock = { version = "0.4", optional = true }
@@ -147,6 +154,8 @@ landlock = ["sandbox-landlock"]
 probe = ["dep:probe-rs"]
 # rag-pdf = PDF ingestion for datasheet RAG
 rag-pdf = ["dep:pdf-extract"]
+# CloudClaw: Postgres memory backend
+postgres = ["dep:tokio-postgres", "dep:deadpool-postgres", "dep:postgres-types", "dep:pgvector", "dep:tokio-postgres-rustls"]
 [profile.release]
 opt-level = "z"      # Optimize for size
 lto = "thin"         # Lower memory use during release builds
diff --git a/src/config/schema.rs b/src/config/schema.rs
index 84ba630..0216ee8 100644
--- a/src/config/schema.rs
+++ b/src/config/schema.rs
@@ -787,6 +787,14 @@ pub struct MemoryConfig {
     /// None = wait indefinitely (default). Recommended max: 300.
     #[serde(default)]
     pub sqlite_open_timeout_secs: Option<u64>,
+
+    // ── Postgres backend options (CloudClaw) ──────────────────
+    /// Postgres connection URL. Falls back to DATABASE_URL env var.
+    #[serde(default)]
+    pub postgres_url: Option<String>,
+    /// Connection pool size for the Postgres backend.
+    #[serde(default = "default_postgres_pool_size")]
+    pub postgres_pool_size: usize,
 }
 
 fn default_embedding_provider() -> String {
@@ -828,6 +836,9 @@ fn default_response_cache_ttl() -> u32 {
 fn default_response_cache_max() -> usize {
     5_000
 }
+fn default_postgres_pool_size() -> usize {
+    4
+}
 
 impl Default for MemoryConfig {
     fn default() -> Self {
@@ -852,6 +863,8 @@ impl Default for MemoryConfig {
             snapshot_on_hygiene: false,
             auto_hydrate: true,
             sqlite_open_timeout_secs: None,
+            postgres_url: None,
+            postgres_pool_size: default_postgres_pool_size(),
         }
     }
 }
diff --git a/src/memory/backend.rs b/src/memory/backend.rs
index 8ba7ec3..5435e1b 100644
--- a/src/memory/backend.rs
+++ b/src/memory/backend.rs
@@ -4,6 +4,7 @@ pub enum MemoryBackendKind {
     Lucid,
     Markdown,
     None,
+    Postgres,
     Unknown,
 }
 
@@ -54,6 +55,15 @@ const NONE_PROFILE: MemoryBackendProfile = MemoryBackendProfile {
     optional_dependency: false,
 };
 
+const POSTGRES_PROFILE: MemoryBackendProfile = MemoryBackendProfile {
+    key: "postgres",
+    label: "PostgreSQL with pgvector \u{2014} cloud-native, persistent storage",
+    auto_save_default: true,
+    uses_sqlite_hygiene: false,
+    sqlite_based: false,
+    optional_dependency: true,
+};
+
 const CUSTOM_PROFILE: MemoryBackendProfile = MemoryBackendProfile {
     key: "custom",
     label: "Custom backend — extension point",
@@ -63,9 +73,10 @@ const CUSTOM_PROFILE: MemoryBackendProfile = MemoryBackendProfile {
     optional_dependency: false,
 };
 
-const SELECTABLE_MEMORY_BACKENDS: [MemoryBackendProfile; 4] = [
+const SELECTABLE_MEMORY_BACKENDS: [MemoryBackendProfile; 5] = [
     SQLITE_PROFILE,
     LUCID_PROFILE,
+    POSTGRES_PROFILE,
     MARKDOWN_PROFILE,
     NONE_PROFILE,
 ];
@@ -84,6 +95,7 @@ pub fn classify_memory_backend(backend: &str) -> MemoryBackendKind {
         "lucid" => MemoryBackendKind::Lucid,
         "markdown" => MemoryBackendKind::Markdown,
         "none" => MemoryBackendKind::None,
+        "postgres" => MemoryBackendKind::Postgres,
         _ => MemoryBackendKind::Unknown,
     }
 }
@@ -94,6 +106,7 @@ pub fn memory_backend_profile(backend: &str) -> MemoryBackendProfile {
         MemoryBackendKind::Lucid => LUCID_PROFILE,
         MemoryBackendKind::Markdown => MARKDOWN_PROFILE,
         MemoryBackendKind::None => NONE_PROFILE,
+        MemoryBackendKind::Postgres => POSTGRES_PROFILE,
         MemoryBackendKind::Unknown => CUSTOM_PROFILE,
     }
 }
@@ -121,11 +134,12 @@ mod tests {
     #[test]
     fn selectable_backends_are_ordered_for_onboarding() {
         let backends = selectable_memory_backends();
-        assert_eq!(backends.len(), 4);
+        assert_eq!(backends.len(), 5);
         assert_eq!(backends[0].key, "sqlite");
         assert_eq!(backends[1].key, "lucid");
-        assert_eq!(backends[2].key, "markdown");
-        assert_eq!(backends[3].key, "none");
+        assert_eq!(backends[2].key, "postgres");
+        assert_eq!(backends[3].key, "markdown");
+        assert_eq!(backends[4].key, "none");
     }
 
     #[test]
@@ -143,4 +157,21 @@ mod tests {
         assert!(profile.auto_save_default);
         assert!(!profile.uses_sqlite_hygiene);
     }
+
+    #[test]
+    fn classify_postgres_backend() {
+        assert_eq!(
+            classify_memory_backend("postgres"),
+            MemoryBackendKind::Postgres
+        );
+    }
+
+    #[test]
+    fn postgres_profile_is_not_sqlite_based() {
+        let profile = memory_backend_profile("postgres");
+        assert!(!profile.sqlite_based);
+        assert!(!profile.uses_sqlite_hygiene);
+        assert!(profile.optional_dependency);
+        assert!(profile.auto_save_default);
+    }
 }
diff --git a/src/memory/mod.rs b/src/memory/mod.rs
index 6798ee4..43042ec 100644
--- a/src/memory/mod.rs
+++ b/src/memory/mod.rs
@@ -10,6 +10,8 @@ pub mod snapshot;
 pub mod sqlite;
 pub mod traits;
 pub mod vector;
+#[cfg(feature = "postgres")]
+pub mod postgres;
 
 #[allow(unused_imports)]
 pub use backend::{
@@ -24,6 +26,8 @@ pub use sqlite::SqliteMemory;
 pub use traits::Memory;
 #[allow(unused_imports)]
 pub use traits::{MemoryCategory, MemoryEntry};
+#[cfg(feature = "postgres")]
+pub use postgres::PostgresMemory;
 
 use crate::config::MemoryConfig;
 use std::path::Path;
@@ -46,6 +50,12 @@ where
         }
         MemoryBackendKind::Markdown => Ok(Box::new(MarkdownMemory::new(workspace_dir))),
         MemoryBackendKind::None => Ok(Box::new(NoneMemory::new())),
+        MemoryBackendKind::Postgres => {
+            anyhow::bail!(
+                "Postgres memory backend requested but not compiled in. \
+                 Rebuild with: cargo build --features postgres"
+            );
+        }
         MemoryBackendKind::Unknown => {
             tracing::warn!(
                 "Unknown memory backend '{backend_name}'{unknown_context}, falling back to markdown"
@@ -120,6 +130,35 @@ pub fn create_memory(
         Ok(mem)
     }
 
+    #[cfg(feature = "postgres")]
+    if matches!(
+        classify_memory_backend(&config.backend),
+        MemoryBackendKind::Postgres
+    ) {
+        let env_url = std::env::var("DATABASE_URL").ok();
+        let url = config
+            .postgres_url
+            .as_deref()
+            .or(env_url.as_deref())
+            .expect("postgres backend requires postgres_url in config or DATABASE_URL env var");
+        let embedder: Arc<dyn embeddings::EmbeddingProvider> =
+            Arc::from(embeddings::create_embedding_provider(
+                &config.embedding_provider,
+                api_key,
+                &config.embedding_model,
+                config.embedding_dimensions,
+            ));
+        #[allow(clippy::cast_possible_truncation)]
+        return Ok(Box::new(PostgresMemory::new(
+            url,
+            config.postgres_pool_size,
+            embedder,
+            config.vector_weight as f32,
+            config.keyword_weight as f32,
+            config.embedding_cache_size,
+        )?));
+    }
+
     create_memory_with_sqlite_builder(
         &config.backend,
         workspace_dir,
diff --git a/src/onboard/wizard.rs b/src/onboard/wizard.rs
index 3c2e9b1..c82ee9f 100644
--- a/src/onboard/wizard.rs
+++ b/src/onboard/wizard.rs
@@ -289,6 +289,8 @@ fn memory_config_defaults_for_backend(backend: &str) -> MemoryConfig {
         snapshot_on_hygiene: false,
         auto_hydrate: true,
         sqlite_open_timeout_secs: None,
+        postgres_url: None,
+        postgres_pool_size: 4,
     }
 }
 
