{
  "framework": "nextjs",
  "regions": ["iad1"],
  "crons": [{ "path": "/api/v1/heartbeat", "schedule": "* * * * *" }],
  "functions": {
    "app/api/v1/**/*.ts": {
      "maxDuration": 60
    }
  }
}
